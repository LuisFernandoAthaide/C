#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

void main () {
    setlocale(LC_ALL, "");

int a, b;
a = 10;
b = 3;

// Operadores Aritméticos:

    // Soma (+)
    // Subtração (-)
    // Multiplicação(*)
    // Divisão (/)
    // Resto da divisão (%)
    // Valor absoluto (abs()) = Converte o valor para positivo.

// Operadores de Atribuíção
    // =     	x = 5 	  x = 5
    // +=       x += 3 	  x = x + 3
    // -=       x -= 3 	  x = x - 3
    // *=   	x *= 3 	  x = x * 3
    // /=       x /= 3 	  x = x / 3
    // %=    	x %= 3 	  x = x % 3
    // &=     	x &= 3 	  x = x & 3
    // |=       x |= 3 	  x = x | 3
    // ^=      	x ^= 3 	  x = x ^ 3
    // >>=    	x >>= 3   x = x >> 3
    // <<=    	x <<= 3   x = x << 3

// Operadores Relacionais:

    // Igual (==)
    // Diferente (!=)
    // Maior(>)
    // Maior ou igual (>=)
    // Menor (<)
    // Menor ou igual (<=)

// Operadores Lógicos:
    // and (&&) - Retorna verdadeiro, se todas as premissas forem verdadeiras.
    // or (||)  - Retorna verdadeiro, se pelo menos uma premissa for verdadeira.
    // not (!)  - Inverte o resultado.

}
